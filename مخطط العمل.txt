.../emulated/0 $ tree /storage/emulated/0/project
/storage/emulated/0/project
├── Dockerfile
├── package.json
├── railway.json
├── .dockerignore
├── index.html
├── src
│   ├── index.js
│   ├── public
│   │  
│   ├── routes
│   │   └── session.js
│   ├── sessions
│   └── whatsapp.js

#رفع تعديل جديد
git add . && git commit -m "update" && git pull --rebase origin main && git push origin main

# انشا مفتاح جديد
ssh-keygen -t ed25519 -C "beyou12123" -f ~/.ssh/id_ed25519 -N "" && eval "$(ssh-agent -s)" && ssh-add ~/.ssh/id_ed25519 && cat ~/.ssh/id_ed25519.pub 
